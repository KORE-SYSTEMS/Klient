services:
  klient:
    build: .
    image: ghcr.io/kore-systems/klient:latest
    container_name: klient
    restart: unless-stopped
    ports:
      - "8399:3000"
    volumes:
      - klient-data:/app/data
      - klient-uploads:/app/uploads

volumes:
  klient-data:
  klient-uploads:
