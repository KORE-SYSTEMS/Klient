# Auth - generate with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:8399

# Port (default: 8399)
PORT=8399

# Data paths (default: ./data and ./uploads in project directory)
# DATA_PATH=./data
# UPLOADS_PATH=./uploads
